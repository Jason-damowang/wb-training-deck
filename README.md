# WorkBuddy 从「听说」到「会用」· 培训材料

WorkBuddy 基础培训（第一阶段「会用」）演示讲稿。单文件 HTML，可键盘翻页、可全屏，所有截图与数据包都已内嵌，打开即用。

- **在线预览（最新版 v3.1）**：https://jason-damowang.github.io/wb-training-deck/
- **当前版本**：v3.1 · `workbuddy-training-v3.1.html` · **23 页**
- **历史版本**：v3.0（24 页）· v2.0（19 页）· v1.0（20 页，微信收到的原始素材）

## 版本差异

| | v1.0 | v2.0 | v3.0 | v3.1 |
|---|---|---|---|---|
| 页数 | 20 | 19 | 24 | **23** |
| 变化 | 原始素材 | 删「本周作业」页 + 口径修正 | 并入总裁班 deck 08–12（新增 5 页） | 删「培训目标」页；「能力全景」两页后移到「老板分身」之后；案例去客户名 |

逐条改动说明见 [CHANGELOG.md](CHANGELOG.md)。

## v3.1 页面结构（23 页）

| 页 | 内容 | PART |
|---|---|---|
| 1 | 封面 | — |
| 2 | 提问公式：目标 + 材料 + 约束 + 交付物 | 01 开场 |
| 3 | WorkBuddy 是什么 · 给每个岗位配一个数字分身 | 02 它是什么 |
| 4 | 6 个岗位分身 × 11 个行业 | 03 岗位分身 |
| 5–7 | 销售分身 / 财务分身 / 老板分身 | 03 岗位分身 |
| 8 | 11 项核心能力总览（环绕图） | 04 能力全景 |
| 9 | 11 项能力分成四组 | 04 能力全景 |
| 10–20 | 11 项能力逐项（问题 → 三步 → 可照抄提示词 + 截图） | 05 11 项能力逐项 |
| 21–22 | 三类场景演练、一次任务的四个动作 | 06 真实任务演练 |
| 23 | 结尾 | 07 收尾 |

> 注意：v3.1 删掉了原第 2 页「这次培训结束，你应该能做到什么」（4 个可验收目标 + 3 条提示）。现在 deck 里不再有「本次培训的学习目标」这一页，如果需要，可以在开场页或结尾页用一句话补回来。

## 内容来源与改写原则（v3.0 新增的 5 页）

内容来自总裁班演讲 deck（`wb-ceo-deck` v3-2.html）第 08–12 页，**不是复制粘贴**：

1. **色彩与视觉重做**：总裁班版是深蓝底 + 金色（`#0A2239` / `#12365C` / `#C9A063`），培训版是白底 + 薄荷绿（`#00B87C`）。新页复用培训版既有设计变量与组件（`.goal / .tips / .blk / .pill`），只新增少量岗位场景专用组件（`.pods / .chain / .bub / .cap2 / .five / .quote / .src`，源码见 `scripts/v3-pages.html`）。
2. **口径改为全员视角**：去掉「订阅 / SLA / 多租户 SaaS / ROI」等销售话术；保留数据与来源（Salesforce 28%、APQC 6.4 天月结、Asana 60%、Gartner 1,290 万美元），统一放页脚小字。
3. **密度降一档**：与培训版单页 40–80 字的节奏对齐，总裁班版每页 300+ 字压成「要点 + 1 个案例 + 1 句收口」。
4. **客户名已脱敏**：销售分身页案例为「某家纺客户」（原总裁班版为具体客户名），老板分身页为「张总 · 家纺周一决策会」。

## 仓库结构

```
wb-training-deck/
├── workbuddy-training-v3.1.html   # 当前版本（源）
├── workbuddy-training-v3.0.html   # 历史版本
├── workbuddy-training-v2.0.html   # 历史版本
├── workbuddy-training-v1.0.html   # 历史版本（原始素材，勿改）
├── index.html                     # 跳转到当前版本，供 Pages 根路径直接访问
├── docs/                          # 从当前版本派生的提取文本
│   ├── prompts.md                 # 14 条可照抄提示词（逐字提取自 data-pmt 属性）
│   ├── content-extract.md         # 提示词 + 逐页正文的合并提取版
│   └── full-text.txt              # 纯文本（去标签、去 base64）
├── scripts/
│   ├── extract-docs.py            # 重新生成 docs/
│   ├── v3-pages.html              # v3.0 新增页素材 + 新组件 CSS
│   ├── build-v3.0.py              # v2.0 → v3.0 构建（可复现）
│   └── build-v3.1.py              # v3.0 → v3.1 构建（删页 / 移页 / 脱敏 / 重排页码）
├── CHANGELOG.md
└── .nojekyll
```

## 改内容时的注意点

1. **只改当前版本的 HTML**；`docs/` 是派生产物，改完跑 `python3 scripts/extract-docs.py`。
2. **提示词不在 `.txt` 容器里**。页面上的提示词由 `data-pmt` 属性提供、JS 打字机动画渲染，`<div class="txt">` 是空的。改提示词要搜 `data-pmt="`。
3. **截图是内嵌 base64**，共 29 处；v3.0 新增的 5 页没有截图。
4. **加/删/移页要同步的数字**：封面副标题「N 页里」、封面统计页数、每个页脚的 `NN` 与 `/ N`、结尾页「共 N 页」、PART 编号。顶部页码控件 `#pgnum` 由 JS 按实际页数渲染，不用改逻辑。
5. **移动/删除页用脚本，不要手工剪切**：页块之间的注释、末页之后的 1.5 MB 数据包与灯箱脚本都容易漏。参考 `scripts/build-v3.1.py`（它按 `<section>` 切块、重排、重编页脚，并在末页之后把 suffix 接回）。
6. **改完必须做溢出检查**：舞台固定 1280×720，内容超了会被 `.page{overflow:hidden}` 静默裁掉。用 headless Chrome 测每页元素实际底边与卡片下沿的差值（做法见 CHANGELOG 的「校验」）。
7. 版本升级请**另存新文件**并更新 `index.html` 的跳转目标。

## 本地预览与推送

```bash
open workbuddy-training-v3.1.html    # 直接打开，无外部依赖
git-push-github                      # 本机网络下用这个推送（自动走本地代理 127.0.0.1:7892 / 候选 IP）
```
